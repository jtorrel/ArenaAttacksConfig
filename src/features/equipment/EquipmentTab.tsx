// EquipmentTab — placeholder
// Contiendra : la silhouette du personnage + les 4 emplacements cliquables

export function EquipmentTab() {
  return (
    <div style={{ padding: '2rem', color: 'var(--muted)', fontFamily: 'var(--font-display)' }}>
      Équipement — à venir
    </div>
  );
}