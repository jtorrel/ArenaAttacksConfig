// SpellbookTab — placeholder
// Contiendra : la liste des sorts + l'éditeur (forme / type / modifiers)

export function SpellbookTab() {
  return (
    <div style={{ padding: '2rem', color: 'var(--muted)', fontFamily: 'var(--font-display)' }}>
      Grimoire — à venir
    </div>
  );
}
